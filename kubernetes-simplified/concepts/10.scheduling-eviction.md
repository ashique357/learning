# Kubernetes Scheduling and Eviction ⚖️

## What is Kubernetes Scheduling? (Simple Explanation)

Think of Kubernetes scheduling like a **smart hotel booking system**!

### Real-Life Example 🏨
- **Hotel Manager (Scheduler)** - Assigns guests to best available rooms
- **Room Requirements** - Guest needs (size, amenities, location)
- **Hotel Rules** - Some rooms for VIPs, some for regular guests
- **Overbooking** - Sometimes need to move guests to make space
- **Maintenance** - Temporarily close rooms for cleaning

---

## Scheduling Overview 📊

```
⚖️ KUBERNETES HOTEL BOOKING SYSTEM
┌─────────────────────────────────────────────────────────────┐
│                    SCHEDULING PROCESS                       │
│                                                             │
│ 📋 GUEST REQUEST (Pod Creation)                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Guest needs: 2 beds, WiFi, ground floor, pet-friendly  │ │
│ └─────────────────────────────────────────────────────────┘ │
│                              │                             │
│ 🏨 HOTEL MANAGER (Scheduler)                                │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 1. Check available rooms (nodes)                        │ │
│ │ 2. Filter by requirements (predicates)                  │ │
│ │ 3. Score remaining rooms (priorities)                   │ │
│ │ 4. Assign best room (binding)                           │ │
│ └─────────────────────────────────────────────────────────┘ │
│                              │                             │
│ 🏠 AVAILABLE ROOMS (Nodes)                                  │
│ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐            │
│ │🏠 Room A│ │🏠 Room B│ │🏠 Room C│ │🏠 Room D│            │
│ │✅ Match │ │❌ Full  │ │✅ Match │ │❌ No WiFi│            │
│ │Score: 85│ │         │ │Score: 92│ │         │            │
│ └─────────┘ └─────────┘ └─────────┘ └─────────┘            │
│                              │                             │
│                         🎯 BEST CHOICE                      │
│                        (Room C - Score 92)                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 1. Basic Scheduling 🎯

### How Scheduler Works
```yaml
# simple-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: guest-pod
spec:
  containers:
    - name: app
      image: nginx
      resources:
        requests:
          memory: "256Mi"
          cpu: "250m"
```

### Step-by-Step: Basic Scheduling
```bash
# Step 1: Create pod (guest checks in)
kubectl apply -f simple-pod.yaml

# Step 2: Watch scheduling happen
kubectl get pods -o wide -w

# Step 3: See scheduling events
kubectl describe pod guest-pod
```

---

## 2. Node Selection 🏠

### Node Selectors (Room Preferences)
```yaml
# node-selector-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: picky-guest
spec:
  nodeSelector:
    disktype: ssd          # Only rooms with SSD storage
    zone: us-west-1a       # Only rooms in specific zone
  containers:
    - name: app
      image: nginx
```

### Node Affinity (Advanced Preferences)
```yaml
# node-affinity-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: flexible-guest
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
              - key: kubernetes.io/arch
                operator: In
                values: ["amd64"]
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          preference:
            matchExpressions:
              - key: disktype
                operator: In
                values: ["ssd"]
  containers:
    - name: app
      image: nginx
```

---

## 3. Pod Affinity 👥

### Pod Affinity (Stay Together)
```yaml
# pod-affinity.yaml
apiVersion: v1
kind: Pod
metadata:
  name: frontend-pod
spec:
  affinity:
    podAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        - labelSelector:
            matchExpressions:
              - key: app
                operator: In
                values: ["database"]
          topologyKey: kubernetes.io/hostname
  containers:
    - name: frontend
      image: nginx
```

### Pod Anti-Affinity (Stay Apart)
```yaml
# pod-anti-affinity.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app
                    operator: In
                    values: ["web"]
              topologyKey: kubernetes.io/hostname
      containers:
        - name: web
          image: nginx
```

---

## 4. Taints and Tolerations 🚫

### Taints (Room Restrictions)
```bash
# Add taint to node (mark room as special)
kubectl taint nodes node1 special=gpu:NoSchedule

# Remove taint
kubectl taint nodes node1 special=gpu:NoSchedule-
```

### Tolerations (Special Permissions)
```yaml
# toleration-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: special-guest
spec:
  tolerations:
    - key: "special"
      operator: "Equal"
      value: "gpu"
      effect: "NoSchedule"
  containers:
    - name: gpu-app
      image: tensorflow/tensorflow:latest-gpu
```

---

## 5. Priority Classes 🏆

### Priority Classes (VIP Levels)
```yaml
# priority-classes.yaml
apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: vip-priority
value: 1000
globalDefault: false
description: "VIP guest priority"
---
apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: regular-priority
value: 100
globalDefault: true
description: "Regular guest priority"
```

### Using Priority Classes
```yaml
# vip-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: vip-guest
spec:
  priorityClassName: vip-priority
  containers:
    - name: vip-app
      image: nginx
```

---

## 6. Pod Eviction 🚪

### Eviction Reasons
- **Resource pressure** - Node running out of memory/disk
- **Node maintenance** - Planned node shutdown
- **Priority preemption** - Higher priority pod needs space
- **Policy violations** - Pod violates policies

### Eviction Example
```bash
# Simulate resource pressure
kubectl top nodes

# Check eviction events
kubectl get events --field-selector reason=Evicted

# Manual eviction (for maintenance)
kubectl drain node1 --ignore-daemonsets --delete-emptydir-data
```

---

### Advanced Scheduling Features

#### Multi-Zone Scheduling
```yaml
# multi-zone-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: multi-zone-app
spec:
  replicas: 6
  selector:
    matchLabels:
      app: multi-zone-app
  template:
    metadata:
      labels:
        app: multi-zone-app
    spec:
      topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: topology.kubernetes.io/zone
        whenUnsatisfiable: DoNotSchedule
        labelSelector:
          matchLabels:
            app: multi-zone-app
      - maxSkew: 1
        topologyKey: kubernetes.io/hostname
        whenUnsatisfiable: ScheduleAnyway
        labelSelector:
          matchLabels:
            app: multi-zone-app
      containers:
      - name: app
        image: nginx
```

#### Custom Scheduler
```yaml
# custom-scheduler.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: custom-scheduler-config
  namespace: kube-system
data:
  config.yaml: |
    apiVersion: kubescheduler.config.k8s.io/v1beta3
    kind: KubeSchedulerConfiguration
    profiles:
    - schedulerName: pizza-scheduler
      plugins:
        score:
          enabled:
          - name: NodeResourcesFit
          - name: NodeAffinity
          disabled:
          - name: NodeResourcesLeastAllocated
      pluginConfig:
      - name: NodeResourcesFit
        args:
          scoringStrategy:
            type: MostAllocated  # Pack pods tightly
```

---

## 8. Troubleshooting 🔧

### Common Issues
```bash
# Pod stuck in Pending
kubectl describe pod <pod-name>

# Check node resources
kubectl describe nodes

# Check taints and tolerations
kubectl describe node <node-name>

# Check scheduler logs
kubectl logs -n kube-system -l component=kube-scheduler
```

---

## Next Steps 🎯

Great! Now you understand scheduling. Next:
1. **[Cluster Administration](../10-cluster-administration/cluster-administration.md)** - Managing clusters

---

**Remember:** Kubernetes scheduling is like smart hotel management - finding the best room for each guest based on their needs and preferences! ⚖️✨